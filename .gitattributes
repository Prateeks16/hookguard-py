# Normalize to LF in the repo. Scripts here run inside Linux containers, where
# a CRLF shebang line fails with a confusing "no such file or directory".
* text=auto eol=lf

*.sh   text eol=lf
*.py   text eol=lf
*.go   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.toml text eol=lf
