# Copy to .env and fill in real secrets. docker-compose reads this file.
# INTERNAL_SECRET is shared between gateway and upstream (the Gateway signature
# key). The others are the per-provider webhook signing secrets.
INTERNAL_SECRET=change-me-internal
STRIPE_SECRET=whsec_change-me
GITHUB_SECRET=change-me-github
SHOPIFY_SECRET=change-me-shopify
